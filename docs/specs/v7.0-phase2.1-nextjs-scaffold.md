# v7.0 Phase 2.1 — Next.js scaffold + Supabase Auth (Free tier sign-in)

**Status:** spec drafted 2026-05-07
**Origin:** v7.0 Phase 1 shipped the database foundation (PR #115). Phase 2 is "Ingest API + tamper-evident events + CLI integration", which decomposes into three sub-PRs. **This spec covers Phase 2.1 only**: the Next.js + Supabase Auth foundation that 2.2 (ingest endpoints) and 2.3 (CLI integration) build on. NO API endpoints related to ingest in this PR — that's deliberate scope discipline.

## What we're building

A new `apps/web/` directory at the repo root containing a Next.js 16 App Router application that:

- Renders a minimal landing page (signed-out: "Sign in with Google"; signed-in: "Welcome, <email>" + sign-out)
- Handles Supabase Auth Google OAuth via the standard PKCE callback pattern
- Reads/writes session cookies via `@supabase/ssr` server + browser clients
- Refreshes sessions on protected routes via scoped middleware (NOT on every request)
- Deploys to Vercel pointing at `apps/web/` as the project root

The app does NOT yet display run history, dashboards, or any of the v7.0 product surface. Phase 2.1 is **pure foundation** — once it ships, Phase 2.2 adds the ingest API endpoints, Phase 2.3 wires the CLI.

## Why this matters

Three concrete things land that 2.2 + 2.3 + Phases 4-6 all depend on:

1. **A server surface in the repo for the first time.** All v6.x code is CLI-only. Phase 1 added Supabase migrations but no HTTP layer. 2.1 is where that changes.
2. **Auth infrastructure usable by 2.2's ingest endpoints.** The `@supabase/ssr` server client + cookie management is what 2.2's `POST /api/upload-session` will call to identify the user issuing the upload session.
3. **Vercel deployment plumbing shaken out.** Build output, env var conventions, preview deployments — all real-world quirks better discovered with a hello-world page than with the cryptographic-verification surface of 2.2.

## Architecture

```
┌──────────────────────────────────────────────────────┐
│ Browser                                              │
│  - Click "Sign in with Google"                       │
│  - Redirect to Google OAuth                          │
│  - Google redirects to /api/auth/callback?code=...   │
└────────────┬─────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────────────┐
│ apps/web (Next.js 16, deployed on Vercel)            │
│                                                      │
│  middleware.ts — scoped matcher refreshes session    │
│  app/page.tsx — server component, reads session      │
│  app/api/auth/callback/route.ts — PKCE exchange      │
│  app/api/auth/sign-out/route.ts — clear cookies      │
│                                                      │
│  lib/supabase/                                       │
│    server.ts — createServerClient via @supabase/ssr  │
│    client.ts — createBrowserClient                   │
└────────────┬─────────────────────────────────────────┘
             │  REST / Auth
             ▼
┌──────────────────────────────────────────────────────┐
│ Supabase project (shared with Phase 1 RLS suite)     │
│  - auth schema (Google OAuth provider configured)    │
│  - public schema (organizations, memberships, etc.)  │
│  - RLS policies from Phase 1 enforce visibility      │
└──────────────────────────────────────────────────────┘
```

**Stack** (separate codebase from Delegance — claude-autopilot has its own deployment story; codex-pass-1 CRITICAL pushed back on this same point as the v7.0 spec did):

- **Next.js 16 App Router** (matches Delegance + cert suite stack)
- **React 19** (Next.js 16's pinned version)
- **Tailwind CSS v4** (Next.js 16's recommended; no shadcn primitives in 2.1 — keep deps minimal)
- **`@supabase/ssr`** — official Next.js cookie handling
- **`@supabase/supabase-js`** — already in deps from Phase 1
- **TypeScript with `moduleResolution: "bundler"`** — Next.js native; CLI keeps `NodeNext` (separate `tsconfig.json` per workspace, both extending a minimal `tsconfig.base.json`)
- **npm workspaces** — root `package.json` gains `"workspaces": ["apps/*", "packages/*"]`. CLI deps stay where they are; web deps live in `apps/web/package.json`. Shared tool versions (TypeScript, ESLint) come from root.
- **Vercel** as primary deploy target (intentional for this codebase; Dockerfile is a v7.1+ followup if self-hosting is requested)

## Components + data flow

### File structure

| Path | Purpose |
|---|---|
| `apps/web/package.json` | Next.js 16 + React 19 + @supabase/ssr deps; `name: "@delegance/claude-autopilot-web"`, `private: true`, scripts (`dev`, `build`, `start`, `test`, `typecheck`) |
| `apps/web/next.config.ts` | Standalone output for Vercel; `experimental.typedRoutes: true` |
| `apps/web/tsconfig.json` | Extends root `tsconfig.base.json`; `moduleResolution: "bundler"`, `jsx: "preserve"`, `lib: ["dom", "es2024"]` |
| `apps/web/middleware.ts` | Session refresh with strict matcher (excludes `_next/static`, `_next/image`, favicon, public assets, health) |
| `apps/web/app/layout.tsx` | Root layout (HTML + body + global Tailwind) |
| `apps/web/app/page.tsx` | Landing page — server component reads session, renders signed-in/signed-out states |
| `apps/web/app/api/auth/callback/route.ts` | PKCE code exchange; whitelists redirect destinations |
| `apps/web/app/api/auth/sign-out/route.ts` | Clear server + client cookies; redirect to `/` |
| `apps/web/lib/supabase/server.ts` | `createServerClient` factory using `@supabase/ssr` |
| `apps/web/lib/supabase/client.ts` | `createBrowserClient` factory |
| `apps/web/lib/auth/redirect.ts` | `safeRedirect(next: string \| null): string` — whitelists `next` query param origins |
| `apps/web/app/globals.css` | Tailwind v4 base + minimal reset |
| `apps/web/postcss.config.mjs` | Tailwind v4 PostCSS config |
| `apps/web/.env.example` | `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `NEXT_PUBLIC_SITE_URL` documented |
| `apps/web/tests/auth/redirect.test.ts` | safeRedirect unit tests |
| `apps/web/tests/integration/auth-flow.test.ts` | Mocked Supabase callback / sign-out / invalid code |
| `tsconfig.base.json` (new at root) | Strict flags + common opts; both CLI tsconfig and web tsconfig extend this |
| `tsconfig.json` (root, MODIFIED) | CLI's existing config now extends `tsconfig.base.json` |
| `package.json` (root, MODIFIED) | Adds `"workspaces": ["apps/*", "packages/*"]`; explicit `files` allowlist locking npm tarball boundary |
| `vercel.json` (root) | Build command targets `apps/web/`; routes pass through; ignored paths exclude CLI dirs |
| `.github/workflows/web-tests.yml` | Typecheck + Next.js build + auth smoke tests on every PR; checks for required env vars |
| `.github/workflows/npm-tarball-check.yml` | `npm pack --dry-run` + assert `apps/` is excluded |

### Auth flow (PKCE callback)

1. User clicks "Sign in with Google" on `/`.
2. Browser-side `supabase.auth.signInWithOAuth({ provider: 'google', options: { redirectTo: `${origin}/api/auth/callback` }})` → redirects to Google.
3. Google redirects back to `/api/auth/callback?code=<auth_code>&next=<safe_path>`.
4. `app/api/auth/callback/route.ts`:
   - `supabase.auth.exchangeCodeForSession(code)` → sets session cookies
   - On success: `redirect(safeRedirect(next))`
   - On failure (bad code / expired): `redirect('/?error=auth_failed')`
5. Subsequent requests go through `middleware.ts` which refreshes the session if needed (matcher excludes static + healthcheck routes).
6. Server components / route handlers use `lib/supabase/server.ts` to read the authenticated user.

### `safeRedirect` whitelist (codex WARNING)

```typescript
const ALLOWED_REDIRECT_PATHS = [
  /^\/$/,
  /^\/dashboard(\/|$)/,
  /^\/runs(\/|$)/,
  /^\/settings(\/|$)/,
];

export function safeRedirect(next: string | null | undefined): string {
  if (!next) return '/';
  if (next.startsWith('//') || next.includes('://')) return '/';  // reject absolute / scheme-relative
  if (!ALLOWED_REDIRECT_PATHS.some(re => re.test(next))) return '/';
  return next;
}
```

Tested with: valid path, malicious `//evil.com`, scheme-relative `https://attacker.com`, unknown path, null, empty.

### Middleware matcher (codex WARNING)

```typescript
export const config = {
  matcher: [
    // Match all request paths EXCEPT:
    // - /_next/static, /_next/image (build artifacts)
    // - /favicon.ico, public files (.svg, .png, etc.)
    // - /api/health (Vercel health check)
    '/((?!_next/static|_next/image|favicon\\.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$|api/health).*)',
  ],
};
```

Session refresh happens on page navigation, sign-in callback, sign-out, and any `/dashboard/*` / `/api/*` route — but NOT on static asset fetches.

### Cookie handling

Sign-in: `@supabase/ssr` writes httpOnly cookies for `sb-<project>-auth-token`. Cookie names match Supabase's defaults so the browser client and server client agree on the same session.

Sign-out: clear server cookies via `supabase.auth.signOut()` + explicit `cookies.delete()` on the response for any `sb-*` cookie. Test asserts both server and browser sides agree post-signout (no stale session).

## Error handling + testing

### Test files

| File | Purpose | # tests |
|---|---|---|
| `apps/web/tests/auth/redirect.test.ts` | safeRedirect: valid/invalid/null/scheme-relative | 6 |
| `apps/web/tests/integration/auth-callback.test.ts` | mocked Supabase: valid code → session set, invalid code → error redirect, missing code → error | 4 |
| `apps/web/tests/integration/auth-signout.test.ts` | sign-out clears cookies, idempotent (sign out twice doesn't crash) | 2 |
| `apps/web/tests/integration/middleware-matcher.test.ts` | static/image/health paths bypass matcher; page paths hit it | 4 |
| `apps/web/tests/build/typecheck.test.ts` | `tsc --noEmit` returns 0 across `apps/web/` | 1 |

Total: ~17 web tests. All run via `npm run test:web` (workspace-scoped) — separate from the existing `npm test` (CLI suite) and `npm run test:rls` (Phase 1 RLS suite).

### CI workflow

`.github/workflows/web-tests.yml`:

1. Checkout + Node 22 + npm ci (workspaces install both CLI and web deps).
2. **Required env-var validation**: fail with a clear message if `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` are missing. (CI uses Phase 1's local-Supabase keys; production uses Vercel's env config.)
3. `npm run typecheck --workspace apps/web` — strict TS check.
4. `npm run build --workspace apps/web` — Next.js production build (catches missing env at build time, broken route handlers, etc.).
5. `npm run test:web --workspace apps/web` — the 17 tests.
6. **Tarball boundary check**: `npm pack --dry-run` and assert `apps/` does NOT appear. Explicit guard against accidental web sources in the published CLI.

### Failure modes

| Scenario | Expected behavior |
|---|---|
| Missing `NEXT_PUBLIC_SUPABASE_URL` env at build | Build fails with clear message |
| Google OAuth returns `?error=access_denied` | Callback redirects to `/?error=auth_failed` (no crash) |
| Supabase rejects `code` (expired/replay) | `exchangeCodeForSession` throws → callback catches → redirect with error param |
| User passes `?next=//evil.com` | safeRedirect rejects → redirect to `/` |
| User passes `?next=/admin` (path not in allowlist) | safeRedirect rejects → redirect to `/` |
| Session cookie tampered/forged | Supabase Auth rejects on next request → redirect to `/?error=session_invalid` |
| User signs out while another tab is open | Middleware sees no session → redirects to `/`; both tabs sign out cleanly |

## Codex review — pass 1 (approach selection, 2026-05-07)

| # | Severity | Concern | Resolution |
|---|---|---|---|
| 1 | CRITICAL | "Deployment target mismatch (ECS vs Vercel)" | **Pushed back** — codex confused claude-autopilot with Delegance's CLAUDE.md ECS baseline. They're separate repos. Vercel is intentional for v7.0 web surface (matches the Phase 7 cert suite + simpler than ECS for low-traffic SaaS). Dockerfile is a v7.1+ followup if self-hosting is requested. |
| 2 | WARNING | Middleware refresh on every request | Strict matcher excluding `_next/static`, `_next/image`, favicons, public assets, `/api/health`. |
| 3 | WARNING | Monorepo without workspaces causes drift | npm workspaces adopted (`workspaces: ["apps/*", "packages/*"]`); root manages tool versions. |
| 4 | WARNING | TS module resolution must differ per target | `apps/web/tsconfig.json` uses `bundler`; CLI stays `NodeNext`; shared `tsconfig.base.json` has only strictness flags. |
| 5 | WARNING | Open-redirect / cookie hygiene | `safeRedirect` whitelist + explicit cookie clear on sign-out + integration tests for malicious `next` params. |
| 6 | NOTE | `apps/web/` correct convention | Confirmed; reserved `packages/*` for libraries. |
| 7 | NOTE | npm tarball boundary explicit | Root `package.json#files` allowlist + `npm pack --dry-run` CI check. |
| 8 | NOTE | Web CI must validate auth env | `web-tests.yml` validates required env vars + runs `next build` + auth smoke tests. |

## Stabilization criteria

Phase 2.1 ships only when:

1. `apps/web/` builds cleanly via `next build` with required env vars set.
2. All 17 web tests pass on Linux + macOS + Windows runners (auth flow, middleware matcher, redirect whitelist, sign-out idempotency).
3. Existing CI green: `ci.yml` (CLI tests), `bin-parity.yml` (npm tarball smoke), `db-tests.yml` (RLS), `delegance-regression.yml`, `adapter-cert.yml`.
4. `npm pack --dry-run` confirms `apps/` is not in the published tarball.
5. Vercel preview deployment succeeds end-to-end against a Vercel-staging Supabase project: sign in with Google → redirect to `/` → see "Welcome, <email>" → sign out → session cleared.
6. `package.json` version bumps to `6.3.0-pre.2` on the `next` dist-tag (latest stays 6.2.2).

## Out of scope (Phase 2.1)

- **API endpoints related to ingest** (POST /api/upload-session, PUT /api/runs/:id/events). v7.0 Phase 2.2.
- **CLI dashboard verbs** (login/logout/status). v7.0 Phase 2.3.
- **Org tier UI** (SSO/SAML, org switcher, audit log viewer, cost reports). v7.0 Phase 5.
- **Stripe checkout / customer portal**. v7.0 Phase 3.
- **Run dashboard / live event replay**. v7.0 Phase 4.
- **Dockerfile for self-hosting**. v7.1+ followup.
- **Shared types between CLI and web**. v7.1+ — workspaces enable this when needed but no shared types yet.

## Open questions

1. **Vercel project setup**: do we want a fresh Vercel project for the autopilot.dev domain, or use a preview-only project for Phase 2.1 and create the production project at Phase 6 launch? Recommend latter — preview-only avoids domain decisions until launch prep.
2. **Tailwind v4 vs v3**: Delegance uses v3 (CLAUDE.md). For autopilot we can pick v4 since it's a fresh codebase and Next.js 16 has first-class v4 support. Recommend v4.
3. **Test runner for web**: Vitest (Next.js's recommended) or stay on `node --test`? Recommend Vitest since Next.js's Test Plan defaults to it and the integration tests need React rendering. CLI keeps `node --test`.
